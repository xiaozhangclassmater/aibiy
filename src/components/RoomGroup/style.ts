import { styled } from "styled-components";

export const RoomGroupWapper = styled.div`
  .room-group-wapper{
    width: 100%;
    height: 100%;
    .product-item-container{
      display: flex;
      flex-wrap: wrap;
      margin: 0 -8px;
    }
  }
`
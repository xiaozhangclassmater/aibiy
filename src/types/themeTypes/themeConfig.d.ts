
interface ThemeConfig {
  primaryColor: string, // 主题色
  themeBorder: string,
  themeBorderHover: string,
  secondColor: string,
  SmallHand: string,
  defaultShadow: string,
  textColor: {
    primaryColor: string
  }
}